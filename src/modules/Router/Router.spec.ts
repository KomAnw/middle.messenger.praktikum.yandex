import { expect } from 'chai';

describe('Router', function () {
  it('Должен быть экземпляром класса Router', () => true);
  it('Должен переводить на следующую страницу', () => true);
  it('Должен переводить на предыдущую страницу', () => true);
});
