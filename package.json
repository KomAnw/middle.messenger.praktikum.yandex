{
  "name": "messanger",
  "version": "1.0.0",
  "description": "",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "clean:output": "rimraf dist",
    "open": "start http://localhost:3000",
    "server": "node server.js",
    "start": "yarn run clean:output && parcel index.html -p 3000 --open",
    "build": "yarn run clean:output && parcel build index.html && yarn run open && yarn run server",
    "deploy": "yarn run clean:output && parcel build index.html",
    "eslint": "eslint --ext .ts,.js .",
    "eslint:fix": "eslint --fix --ext .ts,.js .",
    "stylelint": "stylelint \"**/*.scss\"",
    "stylelint:fix": "stylelint \"**/*.scss\" --fix"
  },
  "alias": {
    "src": "./src"
  },
  "engines": {
    "node": ">=12.0.0 <17.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-inline-string": "2.7.0",
    "@parcel/transformer-sass": "2.7.0",
    "@types/express": "4.17.14",
    "@typescript-eslint/eslint-plugin": "^5.42.0",
    "@typescript-eslint/parser": "^5.42.0",
    "eslint": "8.26.0",
    "eslint-config-google": "0.14.0",
    "parcel": "2.7.0",
    "rimraf": "3.0.2",
    "stylelint": "^14.14.0",
    "stylelint-config-standard-scss": "^6.0.0",
    "stylelint-scss": "4.3.0",
    "typescript": "4.8.4"
  },
  "dependencies": {
    "express": "4.18.2",
    "normalize.css": "8.0.1"
  }
}
